version: "3.8"
services:
  # db:
  #   image: postgres
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_PASSWORD: ${DB_MAIN_PASSWORD}
  #     POSTGRES_DB: recruitsim
  #   # ports:
  #   #   - 5432:5432

  app:
    build: .
    # restart: always
    ports:
      - 127.0.0.1:${APP_LOCAL_PORT}:5000
    # depends_on:
    #   - db
    # volumes:
      # - ./:/app
    environment:
      - FLASK_APP=run:app
      - FLASK_ENV=production
      - FLASK_DEBUG=0

volumes:
  db_data: